local Notification = loadstring(game:HttpGet("https://api.irisapp.ca/Scripts/IrisBetterNotifications.lua"))()

Notification.Notify("Script patched for now", "ik sad cant hack in lego game :(", "", {
Duration = 1000,       
Main = {
    Rounding = true,
}
});
